<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KnightElfServer"
        xmlns:KnightElfLibrary="clr-namespace:KnightElfLibrary;assembly=KnightElfLibrary"
        x:Class="KnightElfServer.MainWindow"
        mc:Ignorable="d"
        Title="KnightElf ServerApp" Height="350" Width="525">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Dictionary.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <local:IPtoStringConverter x:Key="IPtoStringConverter"/>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <DockPanel>
            <ToolBarPanel DockPanel.Dock="Top"
                          Orientation="Horizontal"
                          Background="{DynamicResource {x:Static SystemColors.MenuBarBrushKey}}">
                <Button x:Name="btnSettings" Command="{Binding SetConnectionCommand}"
                        Content="{StaticResource SettingsIcon}"
                        Height="28" Width="28"
                        VerticalAlignment="Center" HorizontalAlignment="Center"
                        BorderBrush="{x:Null}" 
                        Background="{DynamicResource {x:Static SystemColors.MenuBarBrushKey}}"/>
                <Button x:Name="btnConnect" Command="{Binding ConnectCommand}"
                        Content="{StaticResource ConnectIcon}"
                        Height="28" Width="28"
                        HorizontalAlignment="Center" VerticalAlignment="Center" 
                        BorderBrush="{x:Null}"
                        Background="{DynamicResource {x:Static SystemColors.MenuBarBrushKey}}"/>
                <Button x:Name="btnDisconnect" Command="{Binding DisconnectCommand}"
                        Content="{StaticResource DisconnectIcon}"
                        Height="28" Width="28"
                        HorizontalAlignment="Center" VerticalAlignment="Center"
                        BorderBrush="{x:Null}"
                        Background="{DynamicResource {x:Static SystemColors.MenuBarBrushKey}}">
                    <!--Button.Command>
                        <MultiBinding Converter="{StaticResource SMDisconnectConverter}">
                            <Binding Path="SM.State"/>
                            <Binding Path="DisconnectCommand" />
                            <Binding Path="IntWaitCommand" />
                        </MultiBinding>
                    </-->
                </Button>
            </ToolBarPanel>

            <StatusBar DockPanel.Dock="Bottom" Grid.Row="3" Grid.ColumnSpan="2" Height="20" FontSize="10" FlowDirection="RightToLeft">
                <StatusBarItem Content="{StaticResource StatusOffline}"/>
                <Separator Background="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}" />
                <Label x:Name="labelPort"
                       Content="{Binding ConnParams.Port}"
                       VerticalContentAlignment="Stretch" HorizontalContentAlignment="Right"
                       Padding="0" Margin="0.5,0" FlowDirection="LeftToRight" />
                <Label Content="Port:" Padding="0" FlowDirection="LeftToRight"/>
                <Label x:Name="labelIPaddr"
                       Content="{Binding ConnParams.IPaddr, Converter={StaticResource IPtoStringConverter}}"
                       VerticalContentAlignment="Stretch" HorizontalContentAlignment="Right"
                       Padding="0" Margin="0.5,0" FlowDirection="LeftToRight" />
                <Label Content="Selected IP:" Padding="0" FlowDirection="LeftToRight"/>
            </StatusBar>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                
                <GroupBox Header="Clipboard content" ClipToBounds="True" Grid.Column="1" Margin="16.8,54,10.4,9.8" FontSize="10" BorderThickness="0">
                    <Border BorderThickness="1" BorderBrush="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" Margin="10,10,3.6,3">
                        <ScrollViewer>
                            <TextBlock x:Name="tbClipboard" TextWrapping="Wrap"/>
                        </ScrollViewer>
                    </Border>
                </GroupBox>
                <Expander IsExpanded="True" ExpandDirection="Left">
                    <!-- TODO: meh.. fa un po' cagare l'expander -->
                        <GroupBox ClipToBounds="True" Margin="10,54,3.2,9.8" Header="Log console" BorderThickness="0" FontSize="10">
                        <Border BorderThickness="1" BorderBrush="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" Margin="10,10,-2.8,3">
                            <ScrollViewer MinWidth="150">
                                <TextBox x:Name="tbLogger" Background="Black" Foreground="White" TextWrapping="Wrap"/>
                            </ScrollViewer>
                        </Border>
                    </GroupBox>
                </Expander>
            </Grid>

        </DockPanel>
    </Grid>
</Window>
